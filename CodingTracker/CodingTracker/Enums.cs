internal enum UserChoice
{
    View = 1,
    Insert,
    Delete,
    Update,
    Track,
    Report,
    Exit
}

internal enum ReportTimeFrame
{
    ThisYear,
    ThisMonth,
    ThisWeek,
    Custom,
}